/**
 * App 클래스 입니다.
 * @class
 */
const App = function () {
  /**
   * message를 입력받아 출력하는 함수입니다.
   * @param message {string} 출력할 메세지 입니다.
   */
  const printMessage = function (message) {
    console.log(message);
  };
  printMessage();
};

App();
